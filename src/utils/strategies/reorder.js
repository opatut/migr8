export const description = `
  Only applies the migrations that are already applied, but tries to bring
  them in the order they appear in the target specification.
`;

export function resolve() {
  // TODO
  throw new Error('Not implemented');
}
